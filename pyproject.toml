[project]
name = "trl"
version = "0.5"
requires-python = ">=3.9"
dependencies = ["requests"]

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["integration: requires a live local backend and explicit opt-in"]
